post:
  tags:
    - search
  summary: Get shop the look outfit
  description: Retrieve outfit by image url
  operationId: get_outfit_by_image_url
  parameters:
  - name: x-correlation-id
    in: header
    description: correlation id to track requests
    schema:
      type: string
  requestBody:
    description: Curation request body
    content:
      application/json:
        schema:
          $ref: "../../schemas/shop_the_look/fetch_theme_request.yaml"
  responses:
    "200":
      description: Successful Response
      content:
        application/json:
          schema:
            $ref: "../../schemas/shop_the_look/fetch_theme_response.yaml"
    "404":
      description: Not found
      content:
        application/json:
          schema:
            $ref: "../../schemas/error_response.yaml"
